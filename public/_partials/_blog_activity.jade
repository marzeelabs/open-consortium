- function excerpt(content) {
  - if(!content || content.indexOf("<!-- more -->") === -1) return content;
  - else return content.split("<!-- more -->")[0];
- }

- var n = 0

ul.blog-activity
  include _latest_posts
    li.blog-activity__list-item
      a.blog-activity__post(href="/blog/#{ post.slug }")
        .blog-activity__post-date= post.date
        .blog-activity__post-title= post.title
        .blog-activity__post-description!= excerpt(partial('/blog/' + post.slug + '.md'))


a(href='/blog').button.button__blue More posts

